# Secrets - never commit
**/google-credentials.json
tracker/data/password.txt

# Runtime data (persisted on the Pi, not source-controlled)
tracker/data/*.json
# roomreset/ has its own .gitignore for its runtime data

# Node
node_modules/
npm-debug.log*
package-lock.json

# PM2
.pm2/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp
