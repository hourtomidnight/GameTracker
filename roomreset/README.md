# RoomReset

Standalone PWA that walks Hour To Midnight escape room operators through the
room reset process, with progress written live to Google Sheets.

See `docs/` (added during implementation) for setup and deployment
instructions. Design spec and implementation plan live in the
`htm-escape-tracker` repo under `docs/superpowers/specs/` and
`docs/superpowers/plans/`.
